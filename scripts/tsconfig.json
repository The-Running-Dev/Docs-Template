{
  "compilerOptions": {
    // Override settings specific to pre-build script
    "target": "ES2020",
    "module": "ESNext",
    "jsx": "react-jsx",
    "outDir": "./build",
    "rootDir": "..",
    "sourceMap": true,
    "declaration": false,
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "noImplicitReturns": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "skipLibCheck": true,

    // Node.js specific settings
    // "bundler" (not "node"): subzerodev-data-json is exports-map-only and
    // classic "node" resolution can't see its subpaths (e.g. `/node`, `/zod`).
    // This only affects `tsc --noEmit` type-checking (prebuild:check) — the
    // actual pre-build script always runs through tsx, which ignores this
    // file's `module`/`moduleResolution` for its own esbuild-based transform.
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,

    // Path mapping for imports
    "baseUrl": "..",
    "paths": {
      "@/*": ["./src/*"],
      "@config/*": ["./config/*"],
      "@data/*": ["./data/*"],
      "@scripts/*": ["./scripts/*"]
    }
  },
  "include": [
    "*.ts",
    "../config/**/*",
    "../src/entities/**/*",
    "../src/schemas/**/*",
    "../src/config/**/*",
    "../src/components/ThemeSwitcher/models.ts",
    "../src/components/ThemeSwitcher/schema.ts",
    "../src/components/NavBarLinks/models.ts",
    "../src/components/NavBarLinks/schema.ts",
    "../src/components/Projects/constants.ts"
  ],
  "exclude": [
    "node_modules",
    "../build",
    "../artifacts",
    "../.docusaurus",
    "build",
    "../src/components/**/*.tsx",
    "../src/pages/**/*",
    "../src/theme/**/*",
    "../data/index.ts"
  ],
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
